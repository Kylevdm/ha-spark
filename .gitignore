# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling / caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local config & secrets
.env
.env.*
!.env.example

# Local data (SQLite memory store, imported history, etc.)
*.db
*.sqlite
*.sqlite3
data/
# Octopus consumption exports (personal meter data)
usage*.csv
consumption*.csv

# Editor / OS
.vscode/
.idea/
.DS_Store
*Zone.Identifier

# Machine-local Claude Code config (permission grants, environment ids).
# Shared settings belong in .claude/settings.json, curated deliberately.
.claude/settings.local.json

# Handoff / transfer artifacts (git bundles & patches)
*.bundle
*.patch
